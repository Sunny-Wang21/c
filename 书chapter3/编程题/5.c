/*
要求用户按任意次序输入从1到16的所有整数，然后用4*4矩阵的形式将他们显示出来，在计算出每行每列和每条对角线上的和 
*/ 


#include <stdio.h>

int main(void)
{
	int q, w, e, r, t, y, u, i, o, p, a, s, d, f, g, h, row sums, column sums, diagonal sums;
	
	printf("Enter the numbers from 1 to 16 in any order:
	");
	scanf("%d %d %d %d %d %d %d %d %d %d %d %d %d %d %d %d", &q, &w, &e, &r, &t, &y, &u, &i, &o, &p, &a, &s, &d, &f, &g, &h);
	rowsums
	
	
	printf("%d\t%d\t%d\t%d\n%d\t%d\t%d\t%d\n%d\t%d\t%d\t%d\n%d\t%d\t%d\t%d\n", q, w, e, r, t, y, u, i, o, p, a, s, d, f, g, h);
	
	
	
	return 0;
 } 
 /*
 本题太墨迹，弃坑！！！ 
 */
